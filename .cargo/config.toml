# `cargo xtask <cmd>` → runs the dev-orchestration crate (the cargo-xtask pattern).
# `cargo xtask ci` is the full local gate (fmt, clippy -D warnings, build, test, docs, deny).
[alias]
xtask = "run --quiet --package xtask --"
