[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# Dev orchestration (the cargo-xtask pattern). `cargo xtask ci` runs the full local gate — never shipped.
[dependencies]
anyhow.workspace = true
clap.workspace = true

[lints]
workspace = true
